spring:
  application:
    name: env

  cloud:
    config:
      name: env
      discovery:
        enabled: true
        service-id: cloud-config-server
      label: master
      profile: default
      username: user
      password: password
